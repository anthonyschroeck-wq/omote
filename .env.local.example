# Local development — get your key from console.anthropic.com
# This key is ONLY used during local dev (npm run dev)
# In production on Vercel, set ANTHROPIC_API_KEY in the dashboard
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
